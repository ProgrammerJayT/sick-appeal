Sick Applications API documentation

//On login attempt

*First check if student is registered for the academic year
 
	use Illuminate\Support\Facades\Http; //Use the HTTP from Fascade
	 
	$response = Http::withHeaders([ 
			'Accept'=> 'application/json', 
		]) 
		->get('127.0.0.1:8000/api/registrations?studentId[eq]=231584237&year[eq]=2023'); 

	echo $response->body(); //Get status from data response